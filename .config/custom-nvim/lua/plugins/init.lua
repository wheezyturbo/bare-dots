-- This file serves as the entry point for your plugins
-- You can return an empty table and add plugins in separate files
-- Each file in the plugins directory will be automatically loaded

return {
  -- Add your plugins here, or create separate files for organization
  -- Example: { "nvim-lua/plenary.nvim" }
}
